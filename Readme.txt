circ_buf

Same size array, pop push of size_elements
FIFO
circular buffer – write to empty space and where already read
write only where there is space and where already been READ

CircBufCreate
CricBufDestory
CricBufFreeSpace – how much space left to write to
CriBufCapacity – total size of the struct
CircBufRead(void*
CitcBufWrite(void*
CircBufIsEmpty(
